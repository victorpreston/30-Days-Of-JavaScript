/**
 * Push the number 42 to the end of the array
 * Shift a variable from the beginning of the array
 */

var myArray = [ true, "What is the meaning iof life?"];
myArray.push(42);
myArray.shift();

console.log(myArray[0]);
console.log(myArray[1]);
